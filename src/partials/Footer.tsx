import { Section } from 'astro-boilerplate-components';

const Footer = () => (
  <Section>
    <div className="flex items-center justify-center gap-4">shrey</div>
  </Section>
);

export { Footer };
